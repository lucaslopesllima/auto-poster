Acabei de automatizar a publicação no LinkedIn usando a API oficial.

Três blocos principais:
- OAuth para gerar o access_token e descobrir o URN do perfil
- Wrapper da API de Posts (texto e imagem)
- Fila em SQLite + agendador

#automacao #python #linkedinapi
